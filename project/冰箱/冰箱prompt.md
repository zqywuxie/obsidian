```text
请优化现有“实验室冰箱管理系统”的冰箱空间布局与可视化设计，仅针对冰箱结构、抽屉布局、盒子管理与空间交互进行重新设计，不需要重新设计整个系统架构。

系统用于生物医学实验室样本管理，重点提升：
- 冰箱空间利用率
- 样本定位效率
- 抽屉与盒子的可视化
- 操作便捷性
- 冻存结构层级清晰度
- 大量样本情况下的快速检索能力

====================================
冰箱1（Drawer Freezer）
====================================

该冰箱为复合结构冰箱，分为上下两个区域。

------------------------------------
上层区域（开放式存储）
------------------------------------

- 共 2 行大型储物区域
- 不再细分固定小格
- 为开放式存储结构
- 不强制要求精确空间坐标
- 采用“自定义物品卡片”方式管理
- 用户可自由添加、编辑、删除物品记录

适用于：
- 试剂盒
- 离心管架
- 大袋冻存样本
- 耗材
- 临时存储物品

该区域核心目标：
- 重点记录“放了什么”
- 而不是精确到固定位置
- 类似开放货架管理模式

界面设计要求：

- 每一层采用自由布局的大卡片展示
- 支持：
  - 卡片拖拽排序
  - 标签分类
  - 搜索过滤
  - 快速录入
- 不需要严格空间编码
- 允许同一层中自由排列物品卡片

每个物品支持记录：
- 名称
- 类型
- 数量
- 负责人
- 创建时间
- 最近更新时间
- 标签
- 备注
- 图片（可选）
- 二维码（可选）

视觉要求：

- 使用仓储式开放货架视觉
- 支持颜色区分：
  - 试剂
  - 样本
  - 耗材
  - 临时物品
- 支持：
  - 占用率显示
  - 拥挤度显示
  - 最近活跃物品高亮
- 支持按项目分组展示

建议：

- 不使用固定 grid 编码
- 使用自由流式布局（flex/grid）
- 更偏向“物品管理”
  而不是“空间定位”

------------------------------------
下层区域（抽屉式）
------------------------------------

下层采用抽屉式结构。

====================
第一层抽屉区
====================

- 共 6 个抽屉
- 固定布局：
  - 2 × 3
  （2行 × 3列）
- 每个抽屉支持放置多个盒子（Box）

====================
第二层抽屉区
====================

- 共 15 个抽屉
- 固定布局为：
  - 5 × 3
  （5行 × 3列）

每个抽屉要求：

- 可展开
- 支持抽屉动画
- 支持显示：
  - 抽屉编号
  - 当前盒子数量
  - 最大容量
  - 空闲率
  - 是否满载
  - 最近访问时间

视觉要求：

- 抽屉状态颜色区分：
  - 空闲
  - 部分占用
  - 接近满载
  - 已满
- 支持 hover 预览内容
- 支持点击展开盒子视图
- 支持热力图式占用展示

====================================
盒子（Box）管理设计
====================================

每个抽屉内可放置多个盒子。

盒子支持两种模式：

------------------------------------
模式1：精细样本模式
------------------------------------

盒子内部可设置网格：
- 10×10
- 9×9
- 8×12
- 自定义布局

用于记录：
- 单个样本位置
- 冻存管坐标
- barcode
- 样本编号

界面要求：
- 支持二维网格展示
- 支持点击格子查看样本
- 支持颜色标记样本状态
- 支持批量导入

------------------------------------
模式2：简略盒子模式
------------------------------------

不细分内部格子。

仅记录：
- Box ID
- 样本类型
- 项目名称
- 数量
- 负责人
- 创建时间
- 备注

适用于：
- 同类样本整盒存储
- 试剂盒
- 大批量混合样本
- 临时归档盒

====================================
冰箱2（Shelf Freezer）
====================================

该冰箱为简单层架式结构。

- 共 4 行储物层
- 无内部格子
- 直接放置物品

每层要求：

- 显示占用率
- 支持直接拖拽物品
- 支持大卡片式展示
- 支持：
  - 标签
  - 负责人
  - 到期时间
  - 最近访问时间

建议：

- 使用仓储货架风格设计
- 增加颜色标签系统
- 支持按项目分组显示

====================================
整体UI/UX优化要求
====================================

重点优化：

1. 空间可视化
2. 抽屉展开交互
3. 样本快速定位
4. 冰箱结构层级感
5. 大规模样本管理效率

要求：

- 类似 Benchling / Notion / LIMS 风格
- 深色模式兼容
- 极简实验室科技风
- 支持响应式布局
- 支持 hover preview
- 支持拖拽操作
- 支持搜索高亮定位

====================================
重点输出内容
====================================

请重点设计：

1. 冰箱整体布局方案
2. 抽屉可视化结构
3. Box层级展示
4. 空间编码规则
5. UI交互逻辑
6. 热力图展示方案
7. 冰箱占用率展示
8. 样本快速定位方案
9. 大规模样本情况下的优化策略
10. 适合 React + Tailwind 的组件化设计方案

请输出：
- UI结构设计
- 页面布局建议
- 组件划分建议
- 可视化设计方案
- 交互逻辑
- 冰箱编码规范
- 适合前端实现的布局方案
```





1. 抽屉里面的盒子 采用预设好的位置 点击进行新增信息，并且标明抽屉内外位置
2. 添加盒子信息 退出后输入框并没有清洗
3. 样本类型支持新增tag 然后后续可以进行重复使用
4. 关于添加盒子的时候 有一个数量选择删去
5. 
6. 

7. 小孔样本合并
8. 信息字段更改
9. 上层盒子设计




```text
请对现有“实验室冰箱管理系统（Laboratory Freezer Management System）”进行一次完整的 UI/UX 与冰箱空间管理 Redesign。

注意：
- 系统主体功能已经存在
- 本次重点不是重构系统架构
- 而是重新设计：
  - 冰箱空间布局
  - 样本定位逻辑
  - 抽屉与盒子交互
  - 样本孔位管理
  - 真实冰箱映射
  - 大规模样本操作体验
  - 样本分组逻辑
  - 二维可视化界面

系统用于：
- 生物医学实验室
- 孕产相关样本库
- 外周血 / 脐血 / 足底血 等样本管理

要求整体风格：
- Benchling + Notion + LIMS 风格
- 极简科技风
- 深色模式兼容
- 强空间层级感
- 支持高密度数据展示
- 支持快速搜索与快速定位

====================================================
一、冰箱结构设计
====================================================

系统包含两个冰箱。

====================================
冰箱1（Drawer Freezer）
====================================

------------------------------------
上层区域（开放式存储）
------------------------------------

- 共 2 行开放式储物层
- 不使用固定格子
- 不强制精确空间坐标
- 更偏向“物品管理”
- 类似开放货架

支持存放：
- 试剂
- 样本盒
- 耗材
- 临时物品
- 离心管架

核心要求：

如果用户选择：
- “试剂”
- “样本盒”
- “样本”

则其内部管理逻辑需要与下层抽屉中的 Box 完全一致。

即：

上层存放的“样本盒”也支持：
- 盒子管理
- 孔位管理
- 样本录入
- 拖拽
- 二维孔位映射
- 批量导入

因此：

上层不仅仅是简单物品列表，
而是：
- 普通物品模式
- Box模式
两种模式共存。

界面要求：

- 卡片式自由布局
- 支持拖拽排序
- 支持标签分类
- 支持搜索
- 支持最近活跃高亮
- 支持颜色分类

====================================================
下层区域（抽屉式）
====================================================

下层采用抽屉式结构。

------------------------------------
第一层抽屉区
------------------------------------

- 共 6 个抽屉
- 固定布局：
  - 2 行 × 3 列

------------------------------------
第二层抽屉区
------------------------------------

- 共 15 个抽屉
- 固定布局：
  - 5 行 × 3 列

所有抽屉支持：

- 抽屉动画
- hover预览
- 点击展开
- 热力图占用状态
- 空位统计
- 最近访问时间

每个抽屉内：

- 可放多个 Box
- Box 支持自由拖拽排序
- Box 支持二维布局展示
- Box 支持缩略图模式
- Box 支持批量移动

要求：

用户能够通过拖拽：
- 调整 Box 顺序
- 更换 Box 所属抽屉
- 批量移动多个 Box

界面类似：
- 文件管理器
- Kanban
- 仓储货架系统

====================================================
二、Box（样本盒）Redesign
====================================================

每个 Box 支持两种模式：

------------------------------------
模式1：孔位模式（核心）
------------------------------------

用于：
- 冻存管
- 单细胞样本
- 血液样本
- DNA/RNA样本

支持：
- 10×10
- 9×9
- 8×12
- 自定义孔位布局

要求：

- 使用二维孔位可视化
- 每个孔位支持点击
- 支持颜色标记
- 支持 hover 样本信息
- 支持批量导入
- 支持 Excel 导入

====================================================
三、小孔样本“分组管理”设计（重点）
====================================================

一个真实样本可能包含多个试管。

例如：
- 同一个患者
- 同一次采样
- 多个冻存管

这些冻存管虽然占据多个孔位，
但实际上属于：
- 同一个样本实体

因此要求：

系统支持：
“样本主实体 + 多孔位子试管”结构。

即：

Sample Group（主样本）
    ├── Tube A
    ├── Tube B
    ├── Tube C

功能要求：

- 多个孔位可归属于同一个主样本
- 主样本支持折叠/展开
- 支持统一颜色标记
- 支持统一搜索
- 支持统一移动
- 支持统一标签

例如：

一个孕妇样本：
- A01
- A02
- A03

都属于：
“张三 - 孕中期外周血”

界面要求：

- 主样本显示为 Group
- 子试管显示为小节点
- 支持连线
- 支持 grouped highlight
- 支持 grouped hover

====================================================
四、样本字段 Redesign（核心）
====================================================

请重新设计样本信息字段。

不要使用传统科研字段。

核心字段改为：

基础信息：
- 姓名
- 编号
- 性别（可选）
- 年龄（可选）

样本来源：
- 门诊
- 产房
- 住院部

样本类型：
- 外周血
- 脐血
- 足底血
- 其他（可扩展）

采集阶段：
- 早孕期
- 中孕期
- 晚孕期
- 产后
- 产后48h

其他：
- 采集时间
- 创建时间
- 负责人
- 标签
- 备注

====================================================
五、Excel 批量导入设计（重点）
====================================================

系统需要支持：

通过 Excel 批量导入样本信息。

但是：

由于涉及具体孔位，
因此需要设计：

“样本信息 + 孔位映射”结构。

请重点设计：

1. Excel模板结构
2. 孔位字段设计
3. Group样本导入逻辑
4. 自动生成孔位逻辑
5. 冲突检测逻辑
6. 批量覆盖逻辑

建议支持：

字段：
- 冰箱
- 抽屉
- Box
- 孔位
- Group ID
- Tube ID
- 姓名
- 编号
- 样本来源
- 样本类型
- 采集阶段

支持：

一个 Group ID 对应多个 Tube。

====================================================
六、二维冰箱映射界面（重点）
====================================================

目前页面左侧存在空白区域。

希望 redesign：

在左侧增加：
“二维真实冰箱映射图”。

目标：

让用户：
- 能快速定位真实冰箱位置
- 能形成空间记忆
- 能看到真实结构

要求：

左侧：
- 冰箱二维结构图
- 抽屉热力图
- Box分布图

右侧：
- 当前选中的 Box 内容
- 样本孔位详情
- 样本信息表

要求：

左侧点击：
- 抽屉
- Box
- 孔位

右侧同步联动。

视觉要求：

- 类似实验室真实冰箱
- 强空间感
- 支持缩放
- 支持 hover
- 支持高亮路径

例如：

点击样本后：
自动高亮：

冰箱 → 抽屉 → Box → 孔位。

====================================================
七、重点输出内容
====================================================

请重点输出：

1. 整体页面 Redesign
2. 左右布局方案
3. 二维冰箱映射方案
4. 抽屉热力图设计
5. Box可视化设计
6. 孔位可视化设计
7. Group样本设计
8. Excel导入方案
9. 空间编码规范
10. React组件划分
11. Tailwind布局建议
12. 大规模样本性能优化
13. 拖拽交互设计
14. 样本搜索定位交互
15. 用户操作流设计

请输出：
- 页面结构
- UI草图描述
- 组件结构
- 可视化方案
- 交互逻辑
- 数据结构建议
- 适合 React + Tailwind + shadcn/ui 的实现方案
```


```text
请在上述 redesign prompt 中进一步优化以下两点：

====================================================
一、Box 增加数据路径字段
====================================================

每个 Box 增加一个可选字段：

- 数据路径（Data Path）

用途：
- 记录该 Box 对应样本的测序数据路径
- 记录服务器路径
- 记录分析结果路径
- 记录原始数据存放目录
- 记录项目文件夹路径

字段示例：
- /data/project/sample_001/
- /mnt/nas/projectA/blood_samples/
- s3://bucket/project/sample_box_01/
- \\server\project\box01\

要求：
- 该字段为可选
- 可为空
- 支持复制路径
- 支持一键打开/跳转（如系统支持）
- 支持路径备注
- 支持后续按数据路径搜索 Box
- 支持在 Box 详情页中展示

====================================================
二、Excel 导入后的样本暂存区与拖拽入孔位
====================================================

优化 Excel 批量导入逻辑：

用户通过 Excel 导入样本信息后，不要求立即绑定孔位。

导入后，样本先进入右侧“待分配样本区”。

右侧展示：
- 导入的样本列表
- Group样本
- 每个 Group 下的 Tube
- 姓名
- 编号
- 样本来源
- 样本类型
- 采集阶段
- Tube数量
- 导入状态

左侧展示：
- 当前选中的冰箱
- 抽屉
- Box
- 孔位二维图

交互要求：

- 用户可从右侧拖拽样本或 Tube
- 拖到左侧 Box 的具体孔位中
- 支持单个 Tube 拖拽
- 支持整个 Group 拖拽
- Group 拖拽时自动寻找连续空孔位
- 支持拖拽失败提示
- 支持孔位冲突检测
- 支持撤销操作
- 支持批量分配
- 支持自动填充空孔位

视觉要求：

右侧待分配区：
- 使用列表 / 表格 / 卡片混合展示
- Group 可折叠
- 已分配 Tube 自动标记
- 未分配 Tube 高亮
- 支持搜索与筛选

左侧孔位区：
- 空孔位可高亮
- 已占用孔位显示样本缩写
- 拖拽经过时显示可放置状态
- 冲突孔位显示警告
- 同一 Group 的多个 Tube 使用相同颜色标记

最终目标：

Excel 导入负责“录入样本信息”，
拖拽负责“分配实际孔位”。

请将该逻辑整合到整体 redesign prompt 中。
```




1. 去掉映射试图 保留最左侧的就行，目前最左侧的冰箱视图离中间正文有点远，布局观感不行，并且字体也有点小
2. 样本添加后 点击查看没有显示信息
3. excel导出 304 Not Modified
4. 导出样本 窗口 第一列应该是系统字段 然后映射是选择excel中的字段，


定位盒子 MLP 1-22
- MLP 15 也可以定位盒子 



1. （住）MLP 1-21，搜索框 搜索编号如MLP 查找后续 1-21这种格式，然后支持搜索MLP 12也可以查找到。但是目前问题是如果title有中文 无法执行
2. 对于盒子root和创建者支持修改
3. 对于盒子新增上传图片功能（用数据库存储 不用oss）
4. 支持盒子命名为 MLP 1-21 / TLP 23-24 / * (即多样本存储，并且在新增盒子的时候 提示用户命名方式)，然后上面搜索框也支持一个盒子多编号搜索
5. 对于搜索 如果MLP 1-21之间有空格，搜索的时候排除空格 即MLP12就能搜索到了


api.ts:365  POST http://localhost:5173/api/upper-items/ui-1779776590183/images 404 (Not Found)