```text
请优化现有“实验室冰箱管理系统”的冰箱空间布局与可视化设计，仅针对冰箱结构、抽屉布局、盒子管理与空间交互进行重新设计，不需要重新设计整个系统架构。

系统用于生物医学实验室样本管理，重点提升：
- 冰箱空间利用率
- 样本定位效率
- 抽屉与盒子的可视化
- 操作便捷性
- 冻存结构层级清晰度
- 大量样本情况下的快速检索能力

====================================
冰箱1（Drawer Freezer）
====================================

该冰箱为复合结构冰箱，分为上下两个区域。

------------------------------------
上层区域（开放式存储）
------------------------------------

- 共 2 行大型储物区域
- 不再细分固定小格
- 为开放式存储结构
- 不强制要求精确空间坐标
- 采用“自定义物品卡片”方式管理
- 用户可自由添加、编辑、删除物品记录

适用于：
- 试剂盒
- 离心管架
- 大袋冻存样本
- 耗材
- 临时存储物品

该区域核心目标：
- 重点记录“放了什么”
- 而不是精确到固定位置
- 类似开放货架管理模式

界面设计要求：

- 每一层采用自由布局的大卡片展示
- 支持：
  - 卡片拖拽排序
  - 标签分类
  - 搜索过滤
  - 快速录入
- 不需要严格空间编码
- 允许同一层中自由排列物品卡片

每个物品支持记录：
- 名称
- 类型
- 数量
- 负责人
- 创建时间
- 最近更新时间
- 标签
- 备注
- 图片（可选）
- 二维码（可选）

视觉要求：

- 使用仓储式开放货架视觉
- 支持颜色区分：
  - 试剂
  - 样本
  - 耗材
  - 临时物品
- 支持：
  - 占用率显示
  - 拥挤度显示
  - 最近活跃物品高亮
- 支持按项目分组展示

建议：

- 不使用固定 grid 编码
- 使用自由流式布局（flex/grid）
- 更偏向“物品管理”
  而不是“空间定位”

------------------------------------
下层区域（抽屉式）
------------------------------------

下层采用抽屉式结构。

====================
第一层抽屉区
====================

- 共 6 个抽屉
- 固定布局：
  - 2 × 3
  （2行 × 3列）
- 每个抽屉支持放置多个盒子（Box）

====================
第二层抽屉区
====================

- 共 15 个抽屉
- 固定布局为：
  - 5 × 3
  （5行 × 3列）

每个抽屉要求：

- 可展开
- 支持抽屉动画
- 支持显示：
  - 抽屉编号
  - 当前盒子数量
  - 最大容量
  - 空闲率
  - 是否满载
  - 最近访问时间

视觉要求：

- 抽屉状态颜色区分：
  - 空闲
  - 部分占用
  - 接近满载
  - 已满
- 支持 hover 预览内容
- 支持点击展开盒子视图
- 支持热力图式占用展示

====================================
盒子（Box）管理设计
====================================

每个抽屉内可放置多个盒子。

盒子支持两种模式：

------------------------------------
模式1：精细样本模式
------------------------------------

盒子内部可设置网格：
- 10×10
- 9×9
- 8×12
- 自定义布局

用于记录：
- 单个样本位置
- 冻存管坐标
- barcode
- 样本编号

界面要求：
- 支持二维网格展示
- 支持点击格子查看样本
- 支持颜色标记样本状态
- 支持批量导入

------------------------------------
模式2：简略盒子模式
------------------------------------

不细分内部格子。

仅记录：
- Box ID
- 样本类型
- 项目名称
- 数量
- 负责人
- 创建时间
- 备注

适用于：
- 同类样本整盒存储
- 试剂盒
- 大批量混合样本
- 临时归档盒

====================================
冰箱2（Shelf Freezer）
====================================

该冰箱为简单层架式结构。

- 共 4 行储物层
- 无内部格子
- 直接放置物品

每层要求：

- 显示占用率
- 支持直接拖拽物品
- 支持大卡片式展示
- 支持：
  - 标签
  - 负责人
  - 到期时间
  - 最近访问时间

建议：

- 使用仓储货架风格设计
- 增加颜色标签系统
- 支持按项目分组显示

====================================
整体UI/UX优化要求
====================================

重点优化：

1. 空间可视化
2. 抽屉展开交互
3. 样本快速定位
4. 冰箱结构层级感
5. 大规模样本管理效率

要求：

- 类似 Benchling / Notion / LIMS 风格
- 深色模式兼容
- 极简实验室科技风
- 支持响应式布局
- 支持 hover preview
- 支持拖拽操作
- 支持搜索高亮定位

====================================
重点输出内容
====================================

请重点设计：

1. 冰箱整体布局方案
2. 抽屉可视化结构
3. Box层级展示
4. 空间编码规则
5. UI交互逻辑
6. 热力图展示方案
7. 冰箱占用率展示
8. 样本快速定位方案
9. 大规模样本情况下的优化策略
10. 适合 React + Tailwind 的组件化设计方案

请输出：
- UI结构设计
- 页面布局建议
- 组件划分建议
- 可视化设计方案
- 交互逻辑
- 冰箱编码规范
- 适合前端实现的布局方案
```



1. 抽屉里面的盒子 采用预设好的位置 点击进行新增信息，并且标明抽屉内外位置
2. 添加盒子信息 退出后输入框并没有清洗
3. 样本类型支持新增tag 然后后续可以进行重复使用
4. 关于添加盒子的时候 有一个数量选择删去
5. 